version: '3.3'
services:

  wordpress:
    # depends_on:
    #   - db
    image: wordpress:latest
    ports:
      - "8002:80"
    restart: always
    environment:
      WORDPRESS_DB_HOST: host.docker.internal:3306
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: awesome_password
    volumes:
      - .:/var/www/html

volumes:
  .:

